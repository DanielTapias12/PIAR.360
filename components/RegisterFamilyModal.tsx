
// This component is no longer used as family registration is handled via the main registration screen.
// The file is kept to avoid breaking imports but can be fully deleted in a future cleanup.
const RegisterFamilyModal = () => null;
export default RegisterFamilyModal;
